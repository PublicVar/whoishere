<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>
        @entryPointStyles('app')
        @entryPointScripts('app')
    </head>
    <body>
        <main class="container">
            @if(auth.isLoggedIn)
              <p class="text-end"> Hello {{ auth.user.firstname }} <a href="{{ route('logout') }}" class="btn btn-outline-dark btn-sm">Logout</a></p>
            @endif

            @!section('main_content')
        </main>
    </body>
</html>
